import type {Meta, StoryObj} from '@storybook/react'
import {InlineTags} from './InlineTags'

const metadata: Meta<typeof InlineTags> = {component: InlineTags}
const Normal: StoryObj<typeof InlineTags> = {args: { }}

export default metadata
export {Normal}
