import type {Meta, StoryObj} from '@storybook/react'
import {Footer} from './Footer'

const metadata: Meta<typeof Footer> = {component: Footer}
const Normal: StoryObj<typeof Footer> = {args: { }}

export default metadata
export {Normal}
